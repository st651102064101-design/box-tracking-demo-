node_modules/
frontend/node_modules/
.DS_Store

# local dev config + on-disk PGlite database — contains JWT_SECRET, never commit
.env
backend/.env
backend/.pglite-dev/

# Flutter PDA build artifacts (Windows MAX_PATH breaks git stash -u)
pda_flutter/build/
**/*.dex
