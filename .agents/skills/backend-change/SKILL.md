---
name: backend-change
description: Edit Express API or DB on main. Use when changing backend/**. Not for UI-only or PDA-only tasks.
---

# Backend change (main)

Work under `backend/`. Prefer Vitest + typecheck. Use migrations skill patterns when schema changes. Local/PGlite tests are disposable — rerun freely.
