---
name: gate-ui-change
description: Change Thai-submit gate UI (pages, outbound/admin links, public gate assets). Use when editing frontend gate flows. Not for backend APIs.
---

# Gate UI change

Keep changes scoped to the requested gate surface. Preserve existing Thai-submit UX decisions (e.g. hiding reader admin links on outbound) unless the user asks to reverse them. Prefer surgical edits to legacy public assets when those files own the behavior.
