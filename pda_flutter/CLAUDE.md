# PDA Flutter — pointer

On this branch, agent guidance is in root `AGENTS.md` and `.agents/skills/`.
For deep PDA RFID/DataWedge playbooks, prefer the `MOBILE_APP_DEMO` harness.
This file stays short on purpose.
