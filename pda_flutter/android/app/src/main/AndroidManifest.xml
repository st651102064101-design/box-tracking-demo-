<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- This MC3390R unit is stuck on Android 8.1 (API 27, no further OTA
         available). RFIDAPI3 2.0.5.x declares minSdk 29 (Android 10+) and
         fails at runtime here (NPE inside TransportSerial). Downgraded to
         RFIDAPI3 2.0.3.162, whose docs list "Android 8 and above" — keep this
         override regardless, since the library's manifest may still declare
         a minSdk above our app's. -->
    <uses-sdk tools:overrideLibrary="com.zebra.rfid.api3" />

    <!-- REST calls to the BoxTrace backend -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Zebra RFID reader (Bluetooth sled + service enumeration). Harmless for
         the MC3390R integrated reader, required for RFD40/RFD8500 sleds. -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <application
        android:label="BoxTrace PDA"
        android:name="${applicationName}"
        android:icon="@drawable/app_icon"
        android:usesCleartextTraffic="true"
        tools:replace="android:label">

        <!-- Qualcomm RFID service used by some Zebra terminals -->
        <uses-library android:name="com.qti.qrs.qc_sdk" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
