<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Zebra RFID reader (service enumeration + Bluetooth sled fallback).
         Harmless for the MC3390R integrated reader. -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <application
        android:label="RFID HTML"
        android:allowBackup="false"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- Qualcomm RFID service used by some Zebra terminals -->
        <uses-library android:name="com.qti.qrs.qc_sdk" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:windowSoftInputMode="adjustResize|stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
